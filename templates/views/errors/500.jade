extends ../../layouts/pc
block body
    .container
        h1!='Error (500)'
        if 'object' == typeof errorMsg
            div(style='margin: 30px 0;').debug!= htmlStringify(errorMsg)
        else
            p!= utils.textToHTML(errorMsg) || 'Sorry, the server encountered an error.<br><br>Please try again shortly, or contact us if the problem continues.'
            //p!= 'Sorry, the server encountered an error.<br><br>Please try again shortly, or contact us if the problem continues.'
        if err
            hr.mv-2
            h3 Error Details
            pre.mb-2: code
                if 'string' == typeof err
                    != utils.textToHTML(err)
                else
                    != htmlStringify(err)

        p 如果您经常看到这个页面，请与我们联系
